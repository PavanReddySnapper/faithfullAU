{% if block.settings.name != blank %}
  {% if block.settings.url != blank -%}
    <a href="{{ block.settings.url }}">
  {%- endif %}
  {{ block.settings.name }}
  {% if block.settings.url != blank %}</a>{% endif %}
{% endif %}

{% schema %}
{
  "name":"Indicator For Global",
  "settings":[
      {
        "type": "text",
        "id":"name",
        "label": "Name"
      },
     {
            "id": "url",
            "label": "URL",
            "type": "url"
          }
  ],
  "presets": [
    {
         "name": "Indicator For Global"
    }
  ]
}
{% endschema %}
